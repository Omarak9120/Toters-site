---
export const prerender = true;

import Base from "../../layouts/Base.astro";
import JobsList from "../../components/jobs/JobsList.astro";
import enJobs from "../../data/jobs/en.json";

const title = "Careers at Toters";
const desc = "Join us in making it easy to get anything you need.";
---

<Base title={`Toters — ${title}`} description={desc} lang="en" dir="ltr">
  <JobsList jobs={enJobs} lang="en" />
</Base>
